namespace Resgrid.Model
{
	/// <summary>Category of a file attached to a Contact (Contacts plan Phase A, decision 2). Append only.</summary>
	public enum ContactAttachmentTypes
	{
		Document = 0,
		PrePlan = 1,
		FloorPlan = 2,
		SitePhoto = 3,
		SiteDrawing = 4,
		Other = 5
	}
}
