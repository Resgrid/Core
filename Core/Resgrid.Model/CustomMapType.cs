namespace Resgrid.Model
{
	public enum CustomMapType
	{
		Indoor = 0,
		Satellite = 1,
		Schematic = 2,
		Event = 3,
		/// <summary>Larger surface area map, e.g. a campus, fairground, or stadium</summary>
		Area = 4,
		/// <summary>Large geographic district map, e.g. county / emergency management jurisdiction</summary>
		District = 5
	}
}

