
@model Resgrid.Web.Areas.User.Models.Mapping.ImportPOIsView
@inject IStringLocalizer<Resgrid.Localization.Areas.User.Mapping.Mapping> localizer
@{
	ViewBag.Title = "Resgrid | " + localizer["ImportPOIsPageTitle"];
}

@section Styles
{

}

<div class="row wrapper border-bottom white-bg page-heading">
	<div class="col-sm-4">
		<h2>@localizer["ImportPOIsHeader"]</h2>
		<ol class="breadcrumb">
			<li>
				<a asp-controller="Home" asp-action="Dashboard" asp-route-area="User">@commonLocalizer["HomeModule"]</a>
			</li>
			<li>
				<a asp-controller="Mapping" asp-action="Index" asp-route-area="User">@localizer["MappingHeader"]</a>
			</li>
			<li class="active">
				<strong>@localizer["ImportPOIsHeader"]</strong>
			</li>
		</ol>
	</div>
</div>

<div class="row">
	<div class="col-xs-12">
		<div class="wrapper wrapper-content">
			<div class="ibox float-e-margins">
				<div class="ibox-content">
					<form class="form-horizontal" role="form" asp-controller="Mapping" asp-action="ImportPOIs" asp-route-area="User" method="post">

						<div class="row">
							<div class="col-md-8 col-md-offset-1">
								@if (!String.IsNullOrEmpty(Model.Message))
								{
									<div class="alert alert-success">
										@Model.Message
									</div>
								}
								<div asp-validation-summary="All" class="text-danger"></div>
								@Html.AntiForgeryToken()
								@Html.HiddenFor(m => m.TypeId)
							</div>
						</div>

						<div class="form-group">
							<label class="col-sm-2 control-label">
								@localizer["Upload"]
							</label>
							<div class="col-sm-10">
								<input name="fileToUpload" id="fileToUpload" type="file" />
							</div>
						</div>

						<div class="form-group">
							<div class="col-sm-4 col-sm-offset-2">
								<a class="btn btn-white" asp-controller="Mapping" asp-action="POIs" asp-route-area="User">@localizer["Cancel"]</a>
								<button class="btn btn-primary" type="submit">@localizer["ImportPOIsButton"]</button>
							</div>
						</div>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>

@section Scripts
{
	<script src="~/js/app/internal/mapping/resgrid.mapping.importPois.js" type="text/javascript"></script>
}
