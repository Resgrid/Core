@model Resgrid.Web.Areas.User.Models.Personnel.EditRoleModel
@inject IStringLocalizer<Resgrid.Localization.Areas.User.Personnel.Person> localizer
@{
		ViewBag.Title = "Resgrid | " + @localizer["EditRoleHeader"];
}

<div class="row wrapper border-bottom white-bg page-heading">
	<div class="col-sm-4">
		<h2>@localizer["EditRoleHeader"]</h2>
		<ol class="breadcrumb">
			<li>
				<a asp-controller="Home" asp-action="Dashboard" asp-route-area="User">@commonLocalizer["HomeModule"]</a>
			</li>
			<li>
				<a asp-controller="Personnel" asp-action="Index" asp-route-area="User">@commonLocalizer["PersonnelModule"]</a>
			</li>
			<li>
				<a asp-controller="Personnel" asp-action="Roles" asp-route-area="User">@commonLocalizer["Roles"]</a>
			</li>
			<li class="active">
				<strong>@localizer["EditRoleHeader"]</strong>
			</li>
		</ol>
	</div>
</div>

<div class="row">
	<div class="col-xs-12">
		<div class="wrapper wrapper-content">
			<div class="ibox float-e-margins">
				<div class="ibox-content">
					<form class="form-horizontal" role="form" asp-controller="Personnel" asp-action="EditRole" asp-route-area="User" method="post">

						@Html.HiddenFor(x => x.Role.PersonnelRoleId)
						@Html.AntiForgeryToken()
						<div asp-validation-summary="All" class="text-danger"></div>

						<div class="form-group">
							<label class="col-sm-2 control-label">@commonLocalizer["Name"]</label>
							<div class="col-sm-10"><input type="text" class="form-control" placeholder="@localizer["RoleNamePlaceholder"]" asp-for="Role.Name"></div>
						</div>
						<div class="form-group">
							<label class="col-sm-2 control-label">@commonLocalizer["Description"]</label>
							<div class="col-sm-10"><textarea cols="30" rows="10" class="form-control" placeholder="@localizer["RoleDescriptionPlaceholder"]" asp-for="Role.Description"></textarea></div>
						</div>
						<div class="form-group">
							<label class="col-sm-2 control-label">
								@localizer["UsersInRole"]
							</label>
							<div class="col-sm-10">
								<select id="users" name="users"></select>
							</div>
						</div>

						<div class="form-group">
							<div class="col-sm-4 col-sm-offset-2">
								<a class="btn btn-white" asp-controller="Personnel" asp-action="Roles" asp-route-area="User">@commonLocalizer["Cancel"]</a>
								<button class="btn btn-primary" type="submit">@commonLocalizer["Save"]</button>
							</div>
						</div>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>

@section Scripts
{
		<script src="~/js/app/internal/personnel/resgrid.personnel.editRole.js" type="text/javascript"></script>
}
