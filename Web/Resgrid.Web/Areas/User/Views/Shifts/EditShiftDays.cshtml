@using Resgrid.Model
@model Resgrid.Web.Areas.User.Models.Shifts.EditShiftView
@inject IStringLocalizer<Resgrid.Localization.Areas.User.Shifts.Shifts> localizer
@{
	ViewBag.Title = "Resgrid | " + localizer["EditShiftCalendarHeader"];
	Layout = "~/Areas/User/Views/Shared/_UserLayout.cshtml";
}

<style>
	.ui-datepicker {
		width: 100% !important;
		}
</style>

<div class="row wrapper border-bottom white-bg page-heading">
	<div class="col-sm-4">
		<h2>@localizer["EditShiftCalendarHeader"]</h2>
		<ol class="breadcrumb">
			<li>
				<a asp-controller="Home" asp-action="Dashboard" asp-route-area="User">@commonLocalizer["HomeModule"]</a>
			</li>
			<li>
				<a asp-controller="Shifts" asp-action="Index" asp-route-area="User">@localizer["ShiftsHeader"]</a>
			</li>
			<li class="active">
				<strong>@localizer["EditShiftCalendarHeader"]</strong>
			</li>
		</ol>
	</div>
</div>

<div class="row">
	<div class="col-xs-12">
		<div class="wrapper wrapper-content">
			<div class="ibox float-e-margins">
				<div class="ibox-content">
					<form class="form-horizontal" role="form" asp-controller="Shifts" asp-action="EditShiftDays" asp-route-area="User" method="post" id="edit_shift_days" name="edit_shift_days">

						<div class="row">
							<div class="col-md-8 col-md-offset-1">
								@Html.AntiForgeryToken()
								@Html.HiddenFor(m => m.Shift.ShiftId)
								@Html.HiddenFor(m => m.Shift.AssignmentType)
								@Html.HiddenFor(m => m.Shift.ScheduleType)
								@Html.HiddenFor(m => m.Dates)
								<div asp-validation-summary="All" class="text-danger"></div>

								@if (!String.IsNullOrEmpty(Model.Message))
								{
									<div class="alert alert-success">
										@Model.Message
									</div>
								}
							</div>
						</div>

						<div class="form-group">
							<div class="col-sm-12"><div id="manualScheduleCalendar"></div></div>
						</div>

						<div class="form-group">
							<div class="col-sm-4 col-sm-offset-2">
								<a class="btn btn-white" asp-controller="Shifts" asp-action="Index" asp-route-area="User">@localizer["CancelButton"]</a>
								<button class="btn btn-primary" type="submit" onclick="submitForm();">@localizer["UpdateShiftDaysButton"]</button>
							</div>
						</div>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>

@section Scripts
{
	<script src="~/lib/jquery-ui-multidatespicker/jquery-ui.multidatespicker.js"></script>

	<script>
		$(document).ready(function() {
			$.ajax({
				url: '@Url.Action("GetShiftDays", "Shifts", new {Area = "User", shiftId = Model.Shift.ShiftId})',
				contentType: 'application/json',
				type: 'GET'
			}).done(function(data) {
				if (data) {
					var unprocessedDays = [];
					var processedDays = [];

					for (var i = 0; i < data.length; i++) {
						if (data[i].Processed === true) {
							processedDays.push(data[i].Day);
						} else {
							unprocessedDays.push(data[i].Day);
						}
					}

					if (unprocessedDays.length === 0 && processedDays.length === 0) {
						$('#manualScheduleCalendar').multiDatesPicker({

							numberOfMonths: [3, 4]
						});
					} else if (unprocessedDays.length > 0 && processedDays.length === 0) {
						$('#manualScheduleCalendar').multiDatesPicker({
							addDates: unprocessedDays,
							numberOfMonths: [3, 4]
						});
					} else {
						$('#manualScheduleCalendar').multiDatesPicker({
							addDisabledDates: processedDays,
							addDates: unprocessedDays,
							numberOfMonths: [3, 4]
						});
					}

					$('#manualScheduleCalendar').children().first().css('width', '100%');
				}
			});
		});

		function submitForm() {
			var dates = $('#manualScheduleCalendar').multiDatesPicker('getDates');
			$('#Dates').val(dates);
			$("form#edit_shift_days")[0].submit();
			//$("form")[0].submit();
		}
	</script>
}
