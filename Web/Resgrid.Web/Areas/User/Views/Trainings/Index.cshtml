@using Resgrid.Model
@using Resgrid.Web.Helpers
@model Resgrid.Web.Areas.User.Models.Training.TrainingIndexModel
@inject IStringLocalizer<Resgrid.Localization.Areas.User.Trainings.Trainings> localizer
@{
  ViewBag.Title = "Resgrid | " + localizer["TrainingsHeader"];
}

<div class="row wrapper border-bottom white-bg page-heading">
  <div class="col-sm-4">
    <h2>@localizer["TrainingsHeader"]</h2>
    <ol class="breadcrumb">
      <li>
        <a asp-controller="Home" asp-action="Dashboard" asp-route-area="User">@localizer["HomeLink"]</a>
      </li>
      <li class="active">
        <strong>@localizer["TrainingsHeader"]</strong>
      </li>
    </ol>
  </div>
  @if (ClaimsAuthorizationHelper.IsUserDepartmentAdmin())
  {
    <div class="col-sm-8">
      <div class="btn-group top-page-buttons" style="float:right;padding-right:15px;">
        <a title="@localizer["AddTrainingIndexButton"]" class="btn btn-success" asp-controller="Trainings" asp-action="New" asp-route-area="User">@localizer["AddTrainingIndexButton"]</a>
      </div>
    </div>
  }
</div>

<div class="row">
  <div class="col-xs-12">
    <div class="wrapper wrapper-content">
      <div class="ibox float-e-margins">
        <div class="ibox-content">
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>
                    @localizer["NameColumn"]
                  </th>
                  <th style="max-width: 300px;">
                    @localizer["DescriptionColumn"]
                  </th>
                  <th>
                    @localizer["DueDateColumn"]
                  </th>
                  <th>
                    @localizer["ActionColumn"]
                  </th>
                  <th class="span1">
                  </th>
                </tr>
              </thead>
              <tbody>
                @foreach (var t in Model.Trainings)
				{
                  <tr>
                    <td>
                      @t.Name
                    </td>
                    <td style="max-width: 300px;">
                      @Html.Raw(t.Description)
                    </td>
                    <td>
                      @if (t.ToBeCompletedBy != null)
					  {
                        @Html.Raw(t.ToBeCompletedBy.Value.ToShortDateString())
					  }
					  else
					  {
                        @localizer["NoDueDate"]
					  }
                    </td>
                    <td>
                      @if (t.Users.Any(x => x.UserId == ClaimsAuthorizationHelper.GetUserId()))
					  {
						var userRecord = t.Users.FirstOrDefault(x => x.UserId == ClaimsAuthorizationHelper.GetUserId());
						if (userRecord != null)
						{
						  if (!userRecord.Viewed)
						  {
                            @Html.Raw("<a href='" + Url.Action("View", "Trainings", new { Area = "User", trainingId = t.TrainingId }) + "' class='btn btn-xs btn-primary'>" + localizer["ViewTrainingButton"] + "</a>")
						  }
						  else if (userRecord.Complete && (t.Questions == null || t.Questions.Count <= 0))
						  {
                            @Html.Raw("<a href='" + Url.Action("View", "Trainings", new { Area = "User", trainingId = t.TrainingId }) + "' class='btn btn-xs btn-primary'>" + localizer["ViewTrainingButton"] + "</a>")
						  }
						  else if (userRecord.Complete && userRecord.Score >= t.MinimumScore && (t.Questions != null && t.Questions.Count > 0))
						  {
                            @Html.Raw("<a href='" + Url.Action("View", "Trainings", new { Area = "User", trainingId = t.TrainingId }) + "' class='btn btn-xs btn-success'>" + localizer["TrainingCompleteButton"] + "</a>")
						  }
						  else if (userRecord.Complete && userRecord.Score < t.MinimumScore && (t.Questions != null && t.Questions.Count > 0))
						  {
                            @Html.Raw("<a href='" + Url.Action("View", "Trainings", new { Area = "User", trainingId = t.TrainingId }) + "' class='btn btn-xs btn-danger'>" + localizer["TrainingCompleteButton"] + "</a>")
						  }
						  else
						  {
                            @Html.Raw("<a href='" + Url.Action("View", "Trainings", new { Area = "User", trainingId = t.TrainingId }) + "' class='btn btn-xs btn-primary'>" + localizer["ViewTrainingButton"] + "</a>")
						  }
						}
					  }
                    </td>
                    <td>
                      @if (ClaimsAuthorizationHelper.IsUserDepartmentAdmin())
					  {
                        <a href="@Url.Action("Edit", "Trainings", new { area = "User", trainingId = t.TrainingId })" class='btn btn-xs btn-info'>
                          <i class='icon-pencil'></i> @localizer["EditButton"]
                        </a>
                        <a href="@Url.Action("Report", "Trainings", new { area = "User", trainingId = t.TrainingId })" class='btn btn-xs btn-primary'>
                          <i class='icon-eye-open'></i> @localizer["ReportButton"]
                        </a>
                        <a href="@Url.Action("DeleteTraining", "Trainings", new { area = "User", trainingId = t.TrainingId })" class='btn btn-xs btn-danger' data-confirm="@localizer["DeleteTrainingWarning"] @t.Name ?" rel="nofollow">
                          <i class="icon-trash">
                          </i> @localizer["DeleteButton"]
                        </a>
					  }
                    </td>
                  </tr>
				}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

@section Scripts
{
  <script src="~/js/app/internal/training/resgrid.training.index.js"></script>
}
