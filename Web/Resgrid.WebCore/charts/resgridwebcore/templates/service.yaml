apiVersion: v1
kind: Service
metadata:
  name: {{ template "resgridwebcore.fullname" . }}
  labels:
    app: {{ template "resgridwebcore.name" . }}
    chart: {{ template "resgridwebcore.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "resgridwebcore.name" . }}
    release: {{ .Release.Name }}
